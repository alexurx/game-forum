# Игровой форум - MVP

Веб-форум для игрового сообщества с системой ачивок, ролями и профилями пользователей.

## Возможности

- 👤 Регистрация и авторизация пользователей
- 📧 Восстановление пароля на email
- 💬 Создание тем и обсуждений
- 🏆 Система достижений (ачивок)
- 👑 Роли: User, Moderator, Admin
- 🎨 Светлая и темная темы
- 📱 Адаптивный дизайн

## Требования

- Python 3.8+
- pip

## Установка

### 1. Клонируйте репозиторий

```bash
git clone <url-репозитория>
cd game-forum
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```env
SECRET_KEY=your-secret-key-here
DEBUG=True

# Email настройки (по умолчанию используется console backend для тестирования)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@gameforum.local

# Для реальной отправки email через Gmail раскомментируйте:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=your-email@gmail.com
```

### 5. Примените миграции

```bash
python manage.py migrate
```

### 6. Создайте суперпользователя

```bash
python manage.py createsuperuser
```

### 7. Загрузите начальные данные

```bash
python manage.py loaddata initial_data.json
```

### 8. Запустите сервер

```bash
python manage.py runserver
```

Форум будет доступен по адресу: `http://localhost:8000`

## Структура проекта

```
game-forum/
├── forum/              # Основное приложение форума
├── users/              # Приложение пользователей
├── achievements/       # Приложение ачивок
├── config/             # Настройки проекта
├── static/             # Статические файлы
├── templates/          # HTML шаблоны
└── media/              # Загружаемые файлы
```

## Роли пользователей

- **User** - обычный пользователь (создание тем, сообщений)
- **Moderator** - модератор (удаление чужих сообщений, закрытие тем)
- **Admin** - администратор (полный доступ, назначение ролей)

## Ачивки

Система автоматически присваивает достижения:

1. **Новичок** - первый пост
2. **Создатель** - первая тема
3. **Активист** - 10 сообщений
4. **Ветеран** - 50 сообщений
5. **Популярный** - тема набрала 20 ответов

## Настройка форума

Отредактируйте файл `config/forum_config.py`:

```python
FORUM_CONFIG = {
    'name': 'Название форума',
    'description': 'Описание',
    'game_title': 'Название игры',
    'primary_color': '#6366f1',
    'icon': '🎮'
}
```

## Админ-панель

Доступна по адресу: `http://localhost:8000/admin/`

## Разработка

### Создание новых ачивок

Добавьте в `achievements/models.py` и создайте миграцию:

```bash
python manage.py makemigrations
python manage.py migrate
```

### Запуск тестов

```bash
python manage.py test
```

## Лицензия

MIT